#!/usr/bin/env zsh

# replaced by zicompinit/zicompinit_fast in the atload ice
# autoload -Uz compinit && compinit -d "$XDG_CACHE_HOME"/zsh/zcompdump

autoload -Uz bashcompinit && bashcompinit # for aws
